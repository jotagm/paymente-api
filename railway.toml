[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/payment-api"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "payment-api"

[services.variables]
PORT = "8080"
